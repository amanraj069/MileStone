<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Skills & Badges</title>
    <link
      rel="stylesheet"
      href="/css/FreelancerD/static_elements/sidebar.css"
    />
    <link rel="stylesheet" href="/css/FreelancerD/skills_badges_style.css" />
  </head>
  <body>
    <div class="dashboard-wrapper">
      <!-- Sidebar -->
      <%- include('./partials/sidebar', {activePage: 'skills_badges'}) %>

      <!-- Main Content Area -->
      <div class="main-content">
        <!-- Skills & Badges Section -->
        <div id="skills-badges" class="content-section active">
          <div class="header">
            <h2>Skills & Badges</h2>
            <p>
              Earn badges by completing tasks and showcasing your expertise.
            </p>
          </div>

          <div class="skills-badges-content">
            <!-- Skills -->
            <section class="skills-section">
              <h4>Your Skills</h4>
              <div class="skills-list">
                <% skills_badges.skills.forEach(skill => { %>
                <span class="skill-tag"><%= skill %></span>
                <% }); %>
              </div>
              <div class="add-skill">
                <h5>Add a New Skill</h5>
                <form id="add-skill-form">
                  <div class="form-group">
                    <label for="new-skill">Skill Name</label>
                    <input
                      type="text"
                      id="new-skill"
                      name="new-skill"
                      placeholder="Enter skill name"
                      required
                    />
                  </div>
                  <button type="submit" class="add-skill-btn">Add Skill</button>
                </form>
              </div>
            </section>

            <!-- Badges -->
            <section class="badges-section">
              <h4>Your Badges</h4>
              <div class="badges-grid">
                <% skills_badges.badges.forEach(badge => { %>
                <div class="badge-item">
                  <img src="<%= badge.image %>" alt="<%= badge.title %>" />
                  <h5><%= badge.title %></h5>
                  <p><%= badge.description %></p>
                  <span class="badge-date"
                    >Earned: <%= badge.date_earned %></span
                  >
                </div>
                <% }); %>
              </div>
            </section>

            <!-- Badge Progress -->
            <section class="badge-progress-section">
              <h4>Badge Progress</h4>
              <% skills_badges.badge_progress.forEach(progress => { %>
              <div class="badge-progress-item">
                <h5><%= progress.title %></h5>
                <p><%= progress.description %></p>
                <div class="progress-bar">
                  <div
                    class="progress"
                    style="width: <%= progress.percentage %>%"
                  ></div>
                </div>
                <span class="progress-status"><%= progress.progress %></span>
              </div>
              <% }); %>
            </section>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
